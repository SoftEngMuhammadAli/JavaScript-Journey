Task 1:

Create an HTML page with a button. When the button is clicked, change the text of the paragraph element.

Task 2:

Create a page with two images and a button. When the button is clicked, swap the source attribute of the images.

Task 3:

Create a form with input fields and a submit button. Use JavaScript to validate the form and display an error message if the input is invalid.

Task 4:

Create an unordered list. Allow the user to add and remove list items dynamically using buttons.

Task 5:

Build a countdown timer that starts when a button is clicked and updates the display in real-time.

Task 6:

Create a tabbed interface where clicking on different tabs displays different sections without a page reload.

Task 7:

Display a progress bar that updates in real-time, such as during a file download or form submission.

Task 8:

Create a search bar that displays instant search results without requiring a full page reload.